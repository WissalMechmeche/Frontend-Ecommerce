import { ShopShopValidators } from './shop-shop-validators';

describe('ShopShopValidators', () => {
  it('should create an instance', () => {
    expect(new ShopShopValidators()).toBeTruthy();
  });
});
